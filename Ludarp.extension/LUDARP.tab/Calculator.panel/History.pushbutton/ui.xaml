<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📐 LUDARP Calculator History" 
        Height="480" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6F8"
        FontFamily="Segoe UI"
        ResizeMode="CanResize"
        ShowInTaskbar="True">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#E1E4E8"/>
            <Setter Property="Foreground" Value="#24292E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#D9381E"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📐 Calculation History" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Text="Review past calculations, copy results, and export to CSV." FontSize="12" Foreground="#666666" Margin="0,3,0,0"/>
            </StackPanel>
        </Grid>

        <!-- History ListView -->
        <Border Grid.Row="1" CornerRadius="6" BorderThickness="1" BorderBrush="#D1D5DB" Background="White">
            <ListView Name="HistoryList" BorderThickness="0" Background="Transparent" AlternationCount="2">
                <!-- ListView styling -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Height" Value="36"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Timestamp" Width="130" DisplayMemberBinding="{Binding timestamp}"/>
                        <GridViewColumn Header="Element A (Value A)" Width="170" DisplayMemberBinding="{Binding element_a_disp}"/>
                        <GridViewColumn Header="Operation" Width="90" DisplayMemberBinding="{Binding op_disp}"/>
                        <GridViewColumn Header="Element B (Value B)" Width="170" DisplayMemberBinding="{Binding element_b_disp}"/>
                        <GridViewColumn Header="Result" Width="100" DisplayMemberBinding="{Binding result}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer / Action Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left options -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Name="ClearBtn" Content="🧹 Clear All" Style="{StaticResource DangerButton}" Margin="0,0,10,0"/>
            </StackPanel>

            <!-- Right options -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Name="CopyBtn" Content="📋 Copy Result" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                <Button Name="ExportBtn" Content="📤 Export CSV" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                <Button Name="CloseBtn" Content="Close" Style="{StaticResource ModernButton}" Width="80"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
